
import { createRoot } from 'react-dom/client'
import App from './App.tsx'
import './index.css'

// Cria o elemento root para renderizar o React
createRoot(document.getElementById("root")!).render(<App />);
